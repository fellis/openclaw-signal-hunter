FROM python:3.12-slim

WORKDIR /app

RUN apt-get update && apt-get install -y --no-install-recommends curl && rm -rf /var/lib/apt/lists/*

RUN pip install --no-cache-dir \
    psycopg2-binary \
    httpx

COPY core/ ./core/
COPY storage/ ./storage/

CMD ["python", "-m", "core.translate_worker"]
