# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
.venv/
venv/
env/
*.egg-info/
dist/
build/
.eggs/

# Environment
.env

# Node
node_modules/
dist/
*.js.map

# IDE
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Qdrant data
qdrant_storage/

# Docker volumes
pgdata/

# Model cache (bge-m3 is large, should not be in repo)
.cache/

# Runtime config - contains live keywords, rules, credentials. Never overwrite with git pull.
# Use config.example.json as reference; copy to config.json on VPS (Reddit is disabled by default; enable in config if needed).
config.json
